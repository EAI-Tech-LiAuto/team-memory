# 🚀 小白也能懂的知识库使用指南

**适合人群**: 完全没用过Git/GitHub的新手
**学习时间**: 10分钟
**操作时间**: 每次2分钟
**✨ 新特性**: 自动更新网页，无需手动操作！

---

## 📚 目录
1. [第一次使用（只需做一次）](#第一次使用)
2. [每天开始工作前（必做）](#每天开始工作前)
3. [分享你的工作成果（核心操作）](#分享你的工作成果)
4. [常见问题解答](#常见问题解答)
5. [紧急救援](#紧急救援)

---

## 第一次使用（只需做一次）

### 步骤0: 安装Python（新增，用于自动更新网页）

**检查是否已安装**：
1. 打开命令行/终端
   - Windows: 按 `Win + R`，输入 `cmd` 回车
   - macOS: 按 `Command + 空格`，输入 `Terminal` 回车
2. 输入 `python --version` 或 `python3 --version` 回车
3. 如果显示版本号（如 Python 3.11.0），说明已安装
4. 如果提示"不是内部或外部命令"或"command not found"，需要安装

**Windows安装Python**：
1. 访问：https://www.python.org/downloads/
2. 下载Python 3.11或更高版本
3. 双击安装，**重要**: 勾选 "Add Python to PATH"
4. 点击 "Install Now"
5. 安装完成后，打开**新的**命令行窗口，输入：
```bash
pip install pyyaml
```
6. 等待安装完成

**macOS安装Python**：
1. macOS通常预装Python，但版本可能较旧
2. 推荐使用Homebrew安装（如果没有Homebrew，先安装它）：
   ```bash
   # 先安装Homebrew（如果还没安装）
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

   # 安装Python
   brew install python3
   ```
3. 或者访问：https://www.python.org/downloads/macos/
4. 下载Python 3.11或更高版本的macOS安装包
5. 双击.pkg文件安装
6. 安装完成后，打开**新的**终端窗口，输入：
   ```bash
   pip3 install pyyaml
   ```
7. 等待安装完成

**为什么需要Python？**
- 用于自动扫描你添加的对话文件
- 自动更新网页上的内容显示
- 你只需安装一次，之后就是全自动的

### 步骤1: 安装Git（5分钟）

**检查是否已安装**：
1. 打开命令行/终端
   - Windows: 按 `Win + R`，输入 `cmd` 回车
   - macOS: 按 `Command + 空格`，输入 `Terminal` 回车
2. 输入 `git --version` 回车
3. 如果显示版本号（如 git version 2.40.0），说明已安装，跳到步骤2
4. 如果提示"不是内部或外部命令"或"command not found"，需要安装

**Windows安装Git**：
1. 访问：https://git-scm.com/download/win
2. 下载完成后双击安装
3. 一路点"Next"（默认选项即可）
4. 重新打开命令行，输入 `git --version` 确认安装成功

**macOS安装Git**：
1. 访问：https://git-scm.com/download/mac
2. 有三种安装方式，推荐使用Homebrew（最简单）：
   ```bash
   brew install git
   ```
3. 或者下载官方安装包：
   - 点击"Binary installer"链接
   - 下载.dmg文件
   - 双击安装，跟随安装向导
4. 或者通过Xcode命令行工具（会自动触发安装）：
   ```bash
   git --version
   ```
   - 如果没安装，会弹出安装提示，点击"安装"即可
5. 重新打开终端，输入 `git --version` 确认安装成功

### 步骤2: 下载知识库到电脑（3分钟）

**Windows用户：**

**方法一：使用Git Bash（推荐）**
1. 在桌面创建一个文件夹，命名为 `team-memory`
2. 进入文件夹，右键 → 选择"Git Bash Here"（如果没有这个选项，用方法二）
3. 输入以下命令并回车：
```bash
git clone https://github.com/EAI-Tech-LiAuto/team-memory.git .
```
4. 等待下载完成（看到"done"就成功了）

**方法二：使用命令行**
1. 打开命令行（Win + R，输入cmd）
2. 输入：
```bash
cd Desktop
mkdir team-memory
cd team-memory
git clone https://github.com/EAI-Tech-LiAuto/team-memory.git .
```

**macOS用户：**

**使用终端（推荐）**
1. 打开终端（Command + 空格，输入Terminal）
2. 输入以下命令：
```bash
# 进入桌面
cd ~/Desktop

# 创建文件夹
mkdir team-memory

# 进入文件夹
cd team-memory

# 克隆仓库
git clone https://github.com/EAI-Tech-LiAuto/team-memory.git .
```
3. 等待下载完成（看到"done"就成功了）

**💡 小提示**：
- macOS的桌面路径是 `~/Desktop`（~代表你的用户主目录）
- 如果想放在其他地方，把 `~/Desktop` 改成你想要的路径

### 步骤3: 配置你的身份（2分钟）

**为什么要配置？** 这样大家知道是谁更新的内容。

**Windows用户：**
打开命令行，输入以下两行（**把中文替换成你的信息**）：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@lixiang.com"
```

**示例**：
```bash
git config --global user.name "张三"
git config --global user.email "zhangsan@lixiang.com"
```

**macOS用户：**
打开终端，输入以下两行（**把中文替换成你的信息**）：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@lixiang.com"
```

**示例**：
```bash
git config --global user.name "李四"
git config --global user.email "lisi@lixiang.com"
```

**✅ 完成！** 第一次设置到此结束，以后不用再做这些步骤了。

---

## 每天开始工作前（必做）

### 📥 同步最新内容（30秒）

**为什么要做？** 确保你看到的是团队最新的知识。

**Windows用户：**
1. 打开命令行
2. 进入知识库文件夹：
   ```bash
   cd Desktop/team-memory
   ```
3. 同步最新内容：
   ```bash
   git pull
   ```

**macOS用户：**
1. 打开终端
2. 进入知识库文件夹：
   ```bash
   cd ~/Desktop/team-memory
   ```
3. 同步最新内容：
   ```bash
   git pull
   ```

**看到什么算成功？**
- 显示 `Already up to date`（已经是最新的）✅
- 或显示 `Updating xxx...` 然后列出更新的文件 ✅

---

## 分享你的工作成果（核心操作）

### 情况1: 我要分享一段对话记录

**场景**: 你和Claude Code讨论了一个技术问题，想分享给团队。

#### 方法A: 使用快速脚本（最简单）⭐⭐⭐⭐⭐

**Windows用户**：
1. 双击运行 `scripts/add-conversation.bat`
2. 按提示输入：
   - 你的名字（如：张三）
   - 话题名称（如：机械臂设计方案）
   - 简短描述（如：讨论了机械臂的三种设计方案）
   - 标签（如：mechanical-design,robotics）
3. 会自动打开编辑器，把你的对话内容粘贴进去
4. 保存并关闭编辑器
5. **✨ 新功能**: 脚本会自动更新网页内容（无需手动操作！）
6. 自动完成所有Git操作并上传
7. **完成！** 1-2分钟后访问网站就能看到你的内容

**macOS/Linux用户**：
1. 打开终端，进入知识库目录：
   ```bash
   cd ~/Desktop/team-memory
   ```
2. 运行脚本：
   ```bash
   ./scripts/add-conversation.sh
   ```
3. 按提示输入：
   - 你的名字（如：李四）
   - 话题名称（如：机械臂设计方案）
   - 简短描述（如：讨论了机械臂的三种设计方案）
   - 标签（如：mechanical-design,robotics）
4. 会自动打开编辑器（通常是nano或vim），把你的对话内容粘贴进去
   - **nano编辑器**：粘贴后按 `Ctrl + X`，然后按 `Y`，最后按回车保存
   - **vim编辑器**：按 `i` 进入编辑模式，粘贴内容，按 `Esc`，输入 `:wq` 保存退出
5. **✨ 新功能**: 脚本会自动更新网页内容（无需手动操作！）
6. 自动完成所有Git操作并上传
7. **完成！** 1-2分钟后访问网站就能看到你的内容

**自动化的魔法 ✨**：
- ✅ 自动扫描你的对话文件
- ✅ 自动提取标题、日期、作者、标签
- ✅ 自动生成网页卡片和时间线
- ✅ 自动更新统计数字
- ✅ 无需手动编辑index.html

**流程图**：
```
你添加对话文件
   ↓
脚本自动扫描并解析
   ↓
自动生成网页内容
   ↓
自动提交到GitHub
   ↓
1-2分钟后网站自动更新
```

#### 方法B: 手动操作（3分钟）

**步骤1: 创建文件**
1. 打开 `conversations/2026-01/` 文件夹
2. 创建一个新文件，命名为：`你的话题名称.md`（如 `机械臂设计.md`）
3. 用记事本或VS Code打开

**步骤2: 填写内容**

复制以下模板，填写你的信息：
```markdown
---
date: 2026-01-28
author: 你的名字
topic: 对话主题
tags: [标签1, 标签2]
status: completed
---

# 对话标题

在这里粘贴你的对话内容...
```

**步骤3: 提交到团队库**

打开命令行，依次输入这3条命令：
```bash
# 命令1: 告诉Git你添加了新文件
git add .

# 命令2: 写一个说明
git commit -m "添加XX讨论 by @你的名字"

# 命令3: 上传到云端
git push
```

**看到什么算成功？**
- `git push` 后显示 `To https://github.com/...`
- 然后显示 `main -> main` ✅

---

## ⚠️ 写文档时的重要提醒（避免链接失效）

### 🎯 核心规则：不要链接到不存在的文件

**问题举例**：
```markdown
❌ 错误写法：
- [导航技术方案](./navigation.md)
  （但navigation.md文件还没创建）
```
**结果**：别人点击后会看到"404 找不到页面"的错误 😢

---

### ✅ 正确的3种做法

#### 做法1：先创建文件，再写链接（最好）⭐⭐⭐⭐⭐

**步骤**：
1. 先创建文件 `navigation.md` 并写好内容
2. 保存文件
3. 再在其他文档里添加链接

**例子**：
```markdown
✅ 正确：
1. 先创建文件 navigation.md
2. 然后在README里写：
   - [导航技术方案](./navigation.md)
```

#### 做法2：写"待补充"，不要写链接 ⭐⭐⭐⭐

**如果文件还没写**，就不要用`[]() `的链接格式，改成普通文字：

```markdown
✅ 正确写法：
- 导航技术方案（待补充）
- 视觉感知（计划中）
- 语音交互（即将添加）
```

这样写，别人就知道"哦，这个还没完成"，不会点击后失望。

#### 做法3：已有文件要用对的路径 ⭐⭐⭐

**路径要看你的文件在哪里**：

**例子1：同一个文件夹**
```
你的文件位置：conversations/2026-01/文件A.md
要链接的文件：conversations/2026-01/文件B.md
写法：[文件B](./文件B.md)  ← 用 ./
```

**例子2：上一层文件夹**
```
你的文件位置：conversations/2026-01/文件A.md
要链接的文件：conversations/文件B.md
写法：[文件B](../文件B.md)  ← 用 ../（往上1层）
```

**例子3：topics文件夹**
```
你的文件位置：conversations/2026-01/文件A.md
要链接的文件：topics/技术方案/README.md
写法：[技术方案](../../topics/技术方案/README.md)
↑ 用 ../../（往上2层到根目录，再进topics）
```

---

### 📝 快速检查清单

**每次写完文档，检查一下**：

```
□ 我写的链接 [xxx](yyy.md)，yyy.md文件存在吗？
□ 如果文件不存在，我改成"xxx（待补充）"了吗？
□ 路径对吗？数一下要往上几层用几个../
```

---

### 🤔 为什么这么重要？

**想象一下**：
- 你写了一个很好的文档 📄
- 里面有10个链接
- 其中5个链接点击后是404错误 ❌
- 别人会觉得："这个知识库好多链接都坏了，不靠谱" 😞

**正确做法的好处**：
- ✅ 所有链接都能打开
- ✅ 别人看到"待补充"知道还在建设中
- ✅ 知识库看起来专业、靠谱 👍

---

### 💡 小技巧

**不确定路径对不对？**

在命令行试试：
```bash
# 进入你文件所在的目录
cd conversations/2026-01

# 看看文件存不存在
ls ../../topics/技术方案/README.md
```
- 如果显示文件名 → 路径对了 ✅
- 如果显示"No such file" → 路径错了或文件不存在 ❌

---

### 情况2: 我要更新已有文件

**场景**: 你想修改或补充某个已有的文档。

**简单3步**：
1. 用编辑器打开文件并修改
2. 保存文件
3. 运行：
```bash
git add .
git commit -m "更新XX内容 by @你的名字"
git push
```

---

## 常见问题解答

### Q1: `git push` 时提示"需要先pull"？

**原因**: 别人在你之前更新了内容。

**解决办法**（复制粘贴这3行）：
```bash
git pull
git add .
git commit -m "合并更新 by @你的名字"
git push
```

### Q2: 提示"merge conflict"（合并冲突）？

**原因**: 你和别人改了同一个文件的同一个地方。

**解决办法**：
1. 打开提示冲突的文件
2. 找到这样的标记：
```
<<<<<<< HEAD
你的内容
=======
别人的内容
>>>>>>> xxx
```
3. 删除标记，只保留正确的内容
4. 保存文件
5. 运行：
```bash
git add .
git commit -m "解决冲突 by @你的名字"
git push
```

**不会解决？** 联系技术同事帮忙，或使用"紧急救援"。

### Q3: 不小心删错了文件怎么办？

**只要没push就能救回来**：
```bash
# 恢复所有未提交的改动
git checkout .
```

**如果已经push了**：
```bash
# 回退到上一个版本
git reset --hard HEAD~1
git push -f
```
⚠️ 使用前请三思，或咨询技术同事！

### Q4: 我想看看我改了什么？

**查看改动**：
```bash
git status          # 查看哪些文件被改了
git diff            # 查看具体改了什么内容
```

### Q5: 忘记自己有没有push了？

**检查方法**：
```bash
git status
```
- 如果显示 `nothing to commit, working tree clean` → 已经push了 ✅
- 如果显示 `Changes not staged...` → 还没push，继续操作

---

## 紧急救援

### 🆘 完全搞不懂了，想重新开始

**警告**: 会丢失你本地所有未提交的改动！

```bash
# 1. 备份你的新文件（重要！）
# 把新创建的文件复制到别的文件夹

# 2. 重置仓库
git reset --hard origin/main
git clean -fd

# 3. 重新同步
git pull

# 4. 把备份的文件复制回来，重新提交
```

### 📞 求助渠道

1. **技术同事**: 找李君、郎博或朱俊蓉
2. **查看详细日志**: `git log` 可以看历史记录
3. **GitHub网页**: https://github.com/EAI-Tech-LiAuto/team-memory 可以直接在网页编辑

---

## 💡 最佳实践

### ✅ 推荐做法
1. **每天开始前先 `git pull`** - 避免冲突
2. **频繁提交** - 小步快跑，不要攒一堆改动
3. **写清楚commit信息** - 方便以后查找
4. **不确定就先备份** - 复制一份到别的地方

### ❌ 避免做法
1. **不要直接删除 `.git` 文件夹** - 这会破坏仓库
2. **不要用中文命名分支** - 容易出问题
3. **不要在多台电脑上同时改** - 容易冲突
4. **不要上传敏感信息** - 密码、密钥等

---

## 🎯 每日工作流程总结

**Windows用户：**
```
早上：
1. 打开命令行
2. cd Desktop/team-memory
3. git pull
   ↓
工作中：有重要对话想分享？
4. 双击 scripts/add-conversation.bat
5. 填写信息，粘贴内容
6. 保存
   ↓
下班前：
7. 检查是否已提交：git status
8. 如果有未提交内容：
   - git add .
   - git commit -m "今日更新 by @你的名字"
   - git push
```

**macOS用户：**
```
早上：
1. 打开终端
2. cd ~/Desktop/team-memory
3. git pull
   ↓
工作中：有重要对话想分享？
4. ./scripts/add-conversation.sh
5. 填写信息，粘贴内容
6. 保存（nano: Ctrl+X, Y, 回车；vim: Esc, :wq）
   ↓
下班前：
7. 检查是否已提交：git status
8. 如果有未提交内容：
   - git add .
   - git commit -m "今日更新 by @你的名字"
   - git push
```

---

## 📱 使用截图说明

**（建议添加实际截图）**

### 截图1: Git Bash打开方式
![](./images/git-bash.png)

### 截图2: 成功pull的样子
![](./images/git-pull-success.png)

### 截图3: 成功push的样子
![](./images/git-push-success.png)

---

**记住**:
- Git不可怕，只是个文件同步工具
- 出错了也能恢复，不要害怕
- 多用几次就熟练了
- 不懂就问，团队随时帮忙！

**祝你使用愉快！** 🎉
