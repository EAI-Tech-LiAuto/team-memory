# 权限测试记录

## 测试目的
验证团队成员的GitHub协作权限是否正确配置。

## 测试成员
- 朱俊蓉 (GitHub: jzhu9324)

## 测试步骤

### 管理员端检查 ✅

1. **确认协作者已添加**
   - 访问：https://github.com/EAI-Tech-LiAuto/team-memory/settings/access
   - 应该能看到 `jzhu9324` 在协作者列表中
   - 权限级别应为：`Write`

2. **确认邀请状态**
   - 如果显示 `Pending`（待接受）→ 需要朱俊蓉接受邀请
   - 如果直接显示在列表中 → 已完成设置

---

### 成员端测试步骤（给朱俊蓉）

**第1步：接受邀请**

方法1：检查邮箱
- 查找GitHub发送的邀请邮件
- 标题：`You've been invited to collaborate on EAI-Tech-LiAuto/team-memory`
- 点击邮件中的 `View invitation` 按钮
- 点击 `Accept invitation`

方法2：直接访问
- 打开：https://github.com/EAI-Tech-LiAuto/team-memory/invitations
- 登录GitHub账号（jzhu9324）
- 点击 `Accept invitation`

**第2步：测试推送权限**

打开命令行（或Git Bash），执行以下命令：

```bash
# 进入仓库目录
cd Desktop/team-memory
# 或你实际克隆的位置：cd 你的路径/team-memory

# 同步最新代码
git pull

# 创建测试文件
echo "权限测试成功 - 朱俊蓉 $(date)" > test-permission-jzhu.txt

# 添加文件
git add test-permission-jzhu.txt

# 提交
git commit -m "test: 测试协作权限 by @jzhu9324"

# 推送（这一步会验证权限）
git push
```

**第3步：查看结果**

✅ **成功的标志**：
```
Enumerating objects: 4, done.
Counting objects: 100% (4/4), done.
Writing objects: 100% (3/3), xxx bytes | xxx KiB/s, done.
Total 3 (delta 0), reused 0 (delta 0)
To https://github.com/EAI-Tech-LiAuto/team-memory.git
   xxxxxxx..yyyyyyy  main -> main
```

❌ **失败的情况**：
```
remote: Permission to EAI-Tech-LiAuto/team-memory.git denied to jzhu9324.
fatal: unable to access 'https://github.com/...': The requested URL returned error: 403
```

---

## 可能遇到的问题及解决方法

### 问题1：提示需要输入用户名和密码

**解决**：
1. 用户名输入：`jzhu9324`
2. 密码输入：GitHub Personal Access Token（不是GitHub登录密码）

**如何获取Token**：
1. 访问：https://github.com/settings/tokens
2. 点击 `Generate new token (classic)`
3. 勾选 `repo` 权限
4. 生成并复制token
5. 将token作为密码输入

### 问题2：提示403 Forbidden

**原因**：可能还没接受邀请，或Git使用了错误的凭证

**解决**：
```bash
# 1. 确认已接受邀请
# 访问：https://github.com/EAI-Tech-LiAuto/team-memory

# 2. 清除旧凭证（Windows）
git credential reject
# 按Ctrl+Z然后回车

# 3. 重新push
git push
# 会要求输入用户名和密码/token
```

### 问题3：如果之前用的不是自己的账号clone的仓库

**解决**：配置正确的用户信息
```bash
# 设置用户名
git config user.name "朱俊蓉"
git config user.email "jzhu9324@github.com"

# 或设置为全局（推荐）
git config --global user.name "朱俊蓉"
git config --global user.email "朱俊蓉的邮箱@lixiang.com"
```

---

## 测试结果记录

### 测试1: 朱俊蓉 (jzhu9324)
- **邀请状态**: □ 已发送 □ 已接受
- **Push测试**: □ 成功 □ 失败
- **测试时间**: __________
- **测试人**: __________
- **备注**: __________

---

## 后续计划

测试成功后：
- [ ] 删除测试文件：`git rm test-permission-jzhu.txt && git commit -m "清理测试文件" && git push`
- [ ] 通知其他成员也测试权限
- [ ] 更新《新手指南》添加权限验证步骤

---

**测试完成后请在下方记录结果** ✍️
