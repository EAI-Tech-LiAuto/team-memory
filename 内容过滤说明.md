# 网页内容过滤说明

## 当前状态

已对网页 `index.html` 进行内容过滤配置，隐藏包含特定关键词的文章。

## 已隐藏的内容

以下内容已在网页上隐藏（源文件保留）：

1. **家用人形机器人1个月工作计划（纯家用场景）**
   - 作者：朱俊蓉
   - 项目负责人：郎咸朋
   - 日期：2026-01-27

2. **人形机器人1个月加速计划（工业+家用双场景）**
   - 作者：郎博
   - 日期：2026-01-27

## 修改的位置

1. **对话记录卡片区域**（第553行和第573行）
   - 添加了 `style="display: none;"` 属性
   - 添加了 `data-filtered-author` 标记

2. **时间线区域**（第696行和第709行）
   - 添加了 `style="display: none;"` 属性
   - 添加了 `data-filtered-author` 标记

3. **统计卡片**（第441行）
   - 对话记录数量：从 6 改为 4

4. **JavaScript过滤逻辑**（第741-832行）
   - 添加了 `HIDDEN_KEYWORDS` 配置：`['郎博', '郎咸朋']`
   - 添加了自动过滤和统计更新功能

## 源文件状态

以下源文件**完全保留**，没有任何修改：

- `conversations/2026-01/home-robot-one-month-plan.md`
- `conversations/2026-01/one-month-accelerated-plan.md`

## 如何恢复显示

### 方法1: 修改JavaScript配置（推荐）

编辑 `index.html` 文件的第741行：

```javascript
// 当前配置（隐藏状态）
const HIDDEN_KEYWORDS = ['郎博', '郎咸朋'];

// 修改为空数组（显示所有内容）
const HIDDEN_KEYWORDS = [];
```

### 方法2: 删除HTML中的隐藏样式

在 `index.html` 中找到以下4处，删除 `style="display: none;"` 和 `data-filtered-author` 属性：

1. 第553行：`<div class="card" ... style="display: none;" data-filtered-author="郎咸朋">`
2. 第573行：`<div class="card" ... style="display: none;" data-filtered-author="郎博">`
3. 第696行：`<div class="timeline-item" style="display: none;" data-filtered-author="郎咸朋">`
4. 第709行：`<div class="timeline-item" style="display: none;" data-filtered-author="郎博">`

同时将统计卡片的数字从4改回6（第441行）。

### 方法3: 临时显示（浏览器开发者工具）

1. 打开浏览器开发者工具（F12）
2. 在Console中输入：

```javascript
document.querySelectorAll('[data-filtered-author]').forEach(el => el.style.display = 'block');
```

3. 这只是临时显示，刷新页面后会恢复隐藏状态

## 添加更多过滤关键词

如果需要隐藏其他人的文章，编辑 `index.html` 第741行：

```javascript
const HIDDEN_KEYWORDS = ['郎博', '郎咸朋', '其他作者名'];
```

## 验证隐藏效果

1. 刷新网页：`Cmd+Shift+R` (Mac) 或 `Ctrl+F5` (Windows)
2. 检查统计卡片：对话记录应显示 **4** 篇
3. 检查对话列表：不应看到上述两篇文章
4. 检查时间线：不应看到上述两篇文章

---

**修改日期**: 2026年2月（根据当前日期）
**修改者**: Claude Code Assistant
**文件状态**: ✅ 已完成
