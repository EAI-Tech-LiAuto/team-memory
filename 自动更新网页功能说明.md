# ✨ 自动更新网页功能说明

## 🎯 解决的问题

**之前的情况**：
- 团队成员添加markdown文件后，需要手动编辑`index.html`
- 需要手动添加卡片HTML、时间线条目、更新统计数字
- 对小白用户不友好，容易出错

**现在的情况**：
- ✅ 添加对话文件后，**自动扫描**并**自动更新**网页
- ✅ 无需手动编辑`index.html`
- ✅ 完全自动化，对小白友好

---

## 🚀 如何使用

### 方法1：使用自动化脚本（推荐）

**Windows用户**：
```bash
# 双击运行
scripts\add-conversation.bat
```

**Mac/Linux用户**：
```bash
./scripts/add-conversation.sh
```

**脚本会自动完成**：
1. 同步最新内容（git pull）
2. 引导你填写信息（姓名、主题、描述、标签）
3. 创建markdown文件并打开编辑器
4. 等你编辑完成后
5. **自动运行更新脚本** - 扫描所有对话文件
6. **自动更新index.html** - 生成卡片和时间线
7. 自动提交并推送到GitHub

**完全不需要手动操作index.html！**

### 方法2：手动添加文件后更新网页

如果你手动创建了markdown文件，可以运行更新脚本：

**Windows**：
```bash
# 方式A: PowerShell脚本（推荐，无需安装依赖）
powershell -ExecutionPolicy Bypass -File scripts\update-index.ps1

# 方式B: Python脚本（需要先安装Python和pyyaml）
python scripts\update-index.py

# 方式C: 使用批处理脚本
scripts\manual-update-web.bat
```

**Mac/Linux**：
```bash
# 方式A: Python脚本
python3 scripts/update-index.py

# 方式B: shell脚本中已集成
./scripts/add-conversation.sh
```

---

## 🔧 工作原理

```
用户添加markdown文件
         ↓
脚本自动调用更新程序
         ↓
    1. 扫描conversations/目录
    2. 读取每个.md文件
    3. 解析YAML frontmatter（日期、作者、标签等）
    4. 提取摘要内容
         ↓
    5. 生成对话卡片HTML
    6. 生成时间线HTML
    7. 更新统计数字
    8. 替换index.html中对应部分
         ↓
    9. git add + commit + push
         ↓
GitHub Pages自动部署（1-2分钟）
         ↓
网站更新完成 ✨
```

---

## 📋 提供的脚本文件

### 1. `scripts/update-index.ps1`
- **PowerShell脚本**，Windows自带，无需安装依赖
- 自动扫描对话文件并更新index.html
- **推荐Windows用户使用**

### 2. `scripts/update-index.py`
- **Python脚本**，跨平台
- 需要安装：`pip install pyyaml`
- 功能与PowerShell版本相同

### 3. `scripts/add-conversation.bat` / `.sh`
- 交互式添加对话记录
- **已集成**自动更新网页功能
- 一键完成所有操作

### 4. `scripts/manual-update-web.bat`
- 手动触发网页更新
- 用于修复或补救

### 5. `scripts/README.md`
- 完整的脚本使用文档
- 包含原理说明和常见问题

---

## ✅ markdown文件格式要求

每个对话文件必须包含YAML frontmatter：

```markdown
---
date: 2026-01-28
author: 你的名字
topic: 对话主题
tags: [标签1, 标签2, 标签3]
related_files: []
status: completed
---

# 对话主题

## 会话背景
这里写背景...

## 关键发现
这里写要点...
```

**脚本会自动提取**：
- `date` → 显示在卡片右上角
- `author` → 显示为贡献者
- `topic` → 显示为卡片标题
- `tags` → 显示为标签列表
- 摘要 → 从"会话背景"或"关键发现"部分自动提取

---

## 💡 对新手用户的建议

### 最简单的方式

1. **安装Git**（一次性）
   - Windows: https://git-scm.com/download/win
   - 默认安装即可

2. **克隆仓库**（一次性）
   ```bash
   cd Desktop
   git clone https://github.com/EAI-Tech-LiAuto/team-memory.git
   ```

3. **每次分享内容时**
   - 双击 `scripts\add-conversation.bat`
   - 按提示填写信息
   - 粘贴对话内容
   - 保存关闭
   - **完成！** 剩下的全自动

### 不需要学习

- ❌ 不需要学Git命令
- ❌ 不需要学HTML
- ❌ 不需要编辑index.html
- ❌ 不需要安装Python（Windows用户）
- ✅ 只需要会填表格和粘贴文本

---

## 🆘 常见问题

### Q1: PowerShell脚本报错怎么办？

**错误**: "无法加载文件，因为在此系统上禁止运行脚本"

**解决**:
```bash
# 以管理员身份打开PowerShell，运行：
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

或者使用Python版本：
```bash
pip install pyyaml
python scripts/update-index.py
```

### Q2: 我添加了文件但网页没显示

**原因**:
- markdown文件缺少YAML frontmatter
- 文件不在`conversations/`目录下
- 自动更新脚本没有运行

**解决**:
```bash
# 手动运行更新脚本
powershell -ExecutionPolicy Bypass -File scripts\update-index.ps1

# 如果成功，提交更新
git add index.html
git commit -m "chore: 更新网页索引"
git push
```

### Q3: 脚本运行后index.html没变化

**检查**:
1. 查看脚本输出，看是否扫描到了文件
2. 检查markdown文件的frontmatter格式是否正确
3. 手动对比index.html文件是否有更新

### Q4: 我想用Python版本但没有Python

**安装Python**:
1. 访问：https://www.python.org/downloads/
2. 下载Python 3.11或更高版本
3. 安装时**勾选** "Add Python to PATH"
4. 打开新的命令行窗口，运行：
   ```bash
   pip install pyyaml
   ```

---

## 📊 更新记录

- **2026-01-28**: 创建自动更新功能
  - 添加PowerShell版本（Windows首选）
  - 添加Python版本（跨平台）
  - 集成到add-conversation脚本
  - 更新新手指南和文档

---

## 📞 需要帮助？

- 查看 [scripts/README.md](./scripts/README.md) - 详细技术文档
- 查看 [新手指南.md](./新手指南.md) - 完整使用教程
- 联系技术同事：李君、郎博、朱俊蓉

---

**祝使用愉快！🎉**
