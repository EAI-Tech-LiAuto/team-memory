# 自动索引更新设置指南

## 问题说明

目前通过在线编辑器添加调研记录后，需要手动运行 `python3 scripts/update-index.py` 才能在网站上看到新记录。

为了实现**自动更新索引**，需要在 GitHub 仓库中添加一个自动化工作流。

---

## 设置步骤（通过 GitHub 网页操作）

### 方式1：直接在 GitHub 网页上创建（推荐）

1. **访问 GitHub 仓库**
   ```
   https://github.com/EAI-Tech-LiAuto/team-memory
   ```

2. **进入 Actions 页面**
   - 点击顶部的 "Actions" 标签
   - 点击 "New workflow" 或 "set up a workflow yourself"

3. **创建新的 workflow 文件**
   - 文件名：`.github/workflows/update-index.yml`
   - 内容：复制下面的完整配置

4. **粘贴配置内容**（见下方）

5. **点击 "Commit changes"** 保存

---

## Workflow 配置内容

```yaml
name: 自动更新索引

on:
  push:
    branches:
      - main
    paths:
      - 'research/**/*.md'
      - 'conversations/**/*.md'

permissions:
  contents: write

jobs:
  update-index:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml

      - name: 运行索引更新脚本
        run: python3 scripts/update-index.py

      - name: 检查是否有变更
        id: check_changes
        run: |
          git diff --quiet index.html || echo "changed=true" >> $GITHUB_OUTPUT

      - name: 提交更新
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add index.html
          git commit -m "chore: 自动更新索引 [skip ci]"
          git push
```

---

## 工作原理

1. **触发条件**：
   - 当 `research/` 或 `conversations/` 下的 `.md` 文件有变更时
   - 自动触发这个工作流

2. **执行步骤**：
   - 检出代码
   - 安装 Python 和依赖
   - 运行 `update-index.py` 更新索引
   - 如果 `index.html` 有变更，自动提交并推送

3. **结果**：
   - 用户通过在线编辑器保存记录
   - 几分钟后，网站自动显示新记录
   - 无需手动运行任何命令

---

## 验证是否生效

设置完成后：

1. **检查 Actions 是否启用**
   - 访问：https://github.com/EAI-Tech-LiAuto/team-memory/actions
   - 应该能看到 "自动更新索引" workflow

2. **测试一下**
   - 通过在线编辑器添加一条测试记录
   - 查看 Actions 页面，应该会自动运行
   - 几分钟后，网站上应该能看到新记录

---

## 如果遇到问题

### 问题1：Actions 没有运行权限

**解决方法**：
1. 访问：https://github.com/EAI-Tech-LiAuto/team-memory/settings/actions
2. 在 "Workflow permissions" 部分
3. 选择 "Read and write permissions"
4. 勾选 "Allow GitHub Actions to create and approve pull requests"
5. 点击 "Save"

### 问题2：看不到 Actions 标签

**原因**：Actions 可能被禁用

**解决方法**：
1. 访问：https://github.com/EAI-Tech-LiAuto/team-memory/settings/actions
2. 确保 "Actions permissions" 设置为 "Allow all actions and reusable workflows"

---

## 临时解决方案（在自动化设置好之前）

如果暂时无法设置自动化，每次添加新记录后：

1. 打开终端
2. 运行：
   ```bash
   cd ~/Desktop/team-memory
   git pull
   python3 scripts/update-index.py
   git add index.html
   git commit -m "chore: 更新索引"
   git push
   ```

---

**设置完成后，以后就完全自动化了！** 🎉
